{$IFNDEF AERO_SILVERSCREEN_INCLUDED}
begin
  WriteLn 'silverscreen.simba is now an optional file, include the one in the "optional" directory from now on because this one will eventually be removed.';
end;
  {$I WaspLib/optional/interfaces/mainscreen/silverscreen.simba}
{$ENDIF}
