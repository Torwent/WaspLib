{$IFNDEF AERO_CRAFTSCREEN_INCLUDED}
begin
  WriteLn 'craftscreen.simba is now an optional file, include the one in the "optional" directory from now on because this one will eventually be removed.';
end;
  {$I WaspLib/optional/interfaces/mainscreen/craftscreen.simba}
{$ENDIF}
